{
  "name": "mre-wry-arm64-deadlock",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal reproducible example: wry WebView2 deadlock on Windows ARM64",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-process": "^2.3.1"
  }
}